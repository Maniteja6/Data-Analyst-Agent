# requirements-worker.txt — additional packages installed in worker images
# Base: requirements.txt (full set)
# This file documents which packages are worker-only for image sizing clarity.
#
# Usage (Dockerfile.worker, Dockerfile.analytics):
#   pip install -r requirements.txt -r requirements-worker.txt

# ── Already in requirements.txt — present here for documentation ──────────────
# prophet==1.1.6
# statsforecast==2.0.1
# xgboost==3.0.2
# scikit-learn==1.7.0
# reportlab==4.4.1
# python-pptx==1.0.2
# lxml==5.4.0
# mlflow==2.22.1

# ── Worker-only extras ────────────────────────────────────────────────────────
# cmdstanpy: required by prophet for Bayesian sampling
cmdstanpy==1.2.4

# joblib: parallel backend for scikit-learn cross-validation
joblib==1.4.2

# patsy: formula interface used by statsforecast internally
patsy==0.5.6

# tenacity: retry decorator used by MLflow and prophet internals
tenacity==9.1.2

# holidays: Prophet holiday calendar support
holidays==0.72
# requirements-dev.txt — development, testing, and notebook dependencies
# Never install in production images.
#
# Usage:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.4.0
pytest-asyncio==0.26.0             # async test support (asyncio mode = auto)
pytest-cov==6.1.0                  # coverage reports
pytest-xdist==3.7.0                # parallel test execution (-n auto)
pytest-mock==3.14.1                # MagicMock + AsyncMock utilities
httpx==0.28.1                      # AsyncClient for FastAPI integration tests
anyio==4.9.0                       # async test backend (used by pytest-asyncio)
aiosqlite==0.21.0                  # SQLite async driver for unit test DATABASE_URL
faker==37.3.0                      # realistic test data generation

# ── Code quality ──────────────────────────────────────────────────────────────
ruff==0.11.13                      # linting + formatting (replaces flake8 + black)
mypy==1.16.0                       # static type checking
types-redis==4.6.0.20241004        # type stubs for redis
types-openpyxl==3.1.5.20250306     # type stubs for openpyxl
pre-commit==4.2.0                  # git hooks for ruff + mypy

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter==1.1.1
nbformat==5.10.4
nbconvert==7.16.6
ipykernel==6.29.5
matplotlib==3.10.3                 # notebook visualisations
seaborn==0.13.2                    # statistical plots in notebooks

# ── Local infrastructure (docker-compose.dev.yml equivalents) ─────────────────
# These are used when running the full stack locally without Docker.
flower==2.0.1                      # Celery task monitor at localhost:5555

# ── Debugging ─────────────────────────────────────────────────────────────────
icecream==2.1.3                    # better print debugging
rich==14.0.0                       # colourful terminal output